@using Offwind.Products.OpenFoam.Models
@model Offwind.WebApp.Areas.CFD.Models.BoundaryConditions.VFieldVectorValue
@{
    var fieldName = ViewData.TemplateInfo.HtmlFieldPrefix;
    var displayName = ViewData.ModelMetadata.DisplayName ?? ViewData.TemplateInfo.HtmlFieldPrefix;
}
<div class="control-group" id="@("EditorFor_" + fieldName)">
    <label class="control-label" for="@fieldName">@displayName <i class="icon-question-sign"></i></label>
    <div class="controls">
        @*        <select class="span2" disabled="" id="@(fieldName + "." + "Type")">
            @{
                foreach (var name in Enum.GetNames(typeof(PatchValueType)))
                {
                    if (name == Model.Type.ToString())
                    {
                        Output.WriteLine("<option selected=\"{0}\"{0}</option>", name);
                    }
                    else
                    {
                        Output.WriteLine("<option>{0}</option>", name);
                    }
                }
            }
        </select>
*@
        <span>@Model.Type.ToString()</span>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value1")" name="@(fieldName + "." + "Value1")" placeholder="0.0" value="@Model.Value1" autocomplete="off" />
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value2")" name="@(fieldName + "." + "Value2")" placeholder="0.0" value="@Model.Value2" autocomplete="off" />
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value3")" name="@(fieldName + "." + "Value3")" placeholder="0.0" value="@Model.Value3" autocomplete="off" />
    </div>
</div>