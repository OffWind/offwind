@model Offwind.WebApp.Areas.CFD.Models.BoundaryConditions.VFieldVectorValue2
@{
    var fieldName = ViewData.TemplateInfo.HtmlFieldPrefix;
    var displayName = ViewData.ModelMetadata.DisplayName ?? ViewData.TemplateInfo.HtmlFieldPrefix;
}
<div class="control-group" id="@("EditorFor_" + fieldName)">
    <label class="control-label" for="@fieldName">@displayName <i class="icon-question-sign"></i></label>
    <div class="controls">
        @*        <select class="span2" disabled="" id="@(fieldName + "." + "Type")">
            @{
                foreach (var name in Enum.GetNames(typeof(PatchValueType)))
                {
                    if (name == Model.Type.ToString())
                    {
                        Output.WriteLine("<option selected=\"{0}\"{0}</option>", name);
                    }
                    else
                    {
                        Output.WriteLine("<option>{0}</option>", name);
                    }
                }
            }
        </select>
*@
        <span>@Model.Type.ToString()</span>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value1")" name="@(fieldName + "." + "Value1")" placeholder="0.0" value="@Model.Value1"/>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value2")" name="@(fieldName + "." + "Value2")" placeholder="0.0" value="@Model.Value2"/>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value3")" name="@(fieldName + "." + "Value3")" placeholder="0.0" value="@Model.Value3"/>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value4")" name="@(fieldName + "." + "Value4")" placeholder="0.0" value="@Model.Value4"/>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value5")" name="@(fieldName + "." + "Value5")" placeholder="0.0" value="@Model.Value5"/>
        <input type="text" class="input-mini inputmask decimal" id="@(fieldName + "." + "Value6")" name="@(fieldName + "." + "Value6")" placeholder="0.0" value="@Model.Value6"/>
    </div>
</div>