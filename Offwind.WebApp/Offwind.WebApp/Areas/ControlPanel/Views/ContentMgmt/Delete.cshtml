@model Offwind.WebApp.Models.DContent

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
@Html.DisplayForModel()

@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index", new { type = Model.DContentType.Id })
    </p>
}
