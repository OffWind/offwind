@model Offwind.WebApp.Areas.WindFarms.Models.VTurbine
<div class="actions-block">
    <button type="submit" class="btn btn-primary">Save</button>
    @if (Model.ReturnTo == "list" || Model.Id == Guid.Empty)
    {
        <a href="@Url.Action("turbines", "HomeWindFarms", new { area = "WindFarms" })" class="btn">Cancel</a>
    }
    else
    {
        <a href="@Url.Action("Details", "Turbine", new { area = "WindFarms", id = Model.Id })" class="btn">Cancel</a>
    }
</div>
